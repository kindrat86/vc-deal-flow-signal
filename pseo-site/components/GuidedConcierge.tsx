// Stub: real component not yet committed on this branch. Returning null so
// the root layout compiles; replace once the actual GuidedConcierge lands.
export default function GuidedConcierge() {
  return null;
}
