<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.gitdealflow.dream-customers</string>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>/Users/sipi/launch-projects/vc-deal-flow-signal/monitoring/dream-customers/run-daily.sh</string>
    </array>

    <!-- Daily at 08:30 local time. Tweak the Hour/Minute if you want a
         different morning slot. Chrome MCP only works while a real Chrome is
         running, so pick a time you usually have a browser open. -->
    <key>StartCalendarInterval</key>
    <dict>
        <key>Hour</key>
        <integer>8</integer>
        <key>Minute</key>
        <integer>30</integer>
    </dict>

    <!-- Don't run a backlog burst if the machine was asleep across the
         scheduled time — one run per nominal slot is plenty. -->
    <key>RunAtLoad</key>
    <false/>

    <key>StandardOutPath</key>
    <string>/Users/sipi/launch-projects/vc-deal-flow-signal/monitoring/dream-customers/launchd.out.log</string>
    <key>StandardErrorPath</key>
    <string>/Users/sipi/launch-projects/vc-deal-flow-signal/monitoring/dream-customers/launchd.err.log</string>

    <key>WorkingDirectory</key>
    <string>/Users/sipi/launch-projects/vc-deal-flow-signal/monitoring/dream-customers</string>

    <!-- Inherit the GUI session's env so claude can find Chrome MCP's native
         host, the user's HOME, etc. -->
    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/Users/sipi/.local/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
    </dict>
</dict>
</plist>
