# Receipts of the Week v1 — Ship Log

**Edition:** 2026-05-06 (Wed)
**Featured dev:** @sindresorhus (Scout rank, 36 pts)
**Generated + committed:** 2026-05-09 (3 days late vs intended Wed-launch — scheduled task fired today)
**Commit:** `39ace81` on branch `claude/seo-round-2-2026-05-09`
**Push:** ff7a09b..39ace81 → origin/claude/seo-round-2-2026-05-09

## Channel posting

| Channel       | Status       | Reason / next action |
| ------------- | ------------ | -------------------- |
| Twitter (@data_nerd) | **NOT POSTED** | Chrome MCP extension not connected (`list_connected_browsers` → `[]`). Ref: `feedback_chrome_mcp_extension_not_connected_2026_05_04.md`. User can paste tweets 1–3 from the draft. Tweet 3 is ~290 chars — needs trim under 280 since @data_nerd is not Premium. |
| Telegram (@gitdealflow) | **NOT POSTED** | Channel is low-sub (per `feedback_telegram_low_sub_skip.md`). Native app requires computer-use + user-present; no Bot API token wired here. User can paste from draft. |
| Substack Notes (The Data Nerd) | **NOT POSTED** | Chrome MCP unavailable (same root cause as Twitter). User can paste single-line note from draft. |
| LinkedIn      | **NOT POSTED — by design** | User-only per `feedback_no_linkedin_actions.md`. Draft ready in main file. |
| Reddit        | **SKIPPED — by design** | No good Receipts subs per task spec. |
| HN            | **SKIPPED — by design** | Per task spec. |

## Why date 2026-05-06 (not 2026-05-13)

The scheduled task fired on Sat 2026-05-09, three days after the intended Wed
launch. Generator default is "next Wednesday" (would have produced 2026-05-13
→ @jamonholmgren), but the task spec explicitly named 2026-05-06.md and the
v1 first-edition framing. Regenerated with `--date=2026-05-06` to produce the
intended Scout-ranked @sindresorhus pick. The May 13 throwaway file was
deleted before commit.

## Recurring weekly task — BLOCKED, user action required

`mcp__scheduled-tasks__create_scheduled_task` returned: `Cannot create scheduled
tasks from within a scheduled task session.` This is a runtime guardrail (a
scheduled task cannot self-spawn another scheduled task).

**User action:** in a fresh interactive session, run:

> Create a recurring scheduled task `receipts-of-the-week-weekly`, cron `7 8 * * 3`
> (Wed 08:07 local), reusing the prompt from the original
> `receipts-of-the-week-may6-launch` SKILL.md but with date references
> swapped for "next Wednesday" (default behavior of the generator), and step 8
> removed.

A ready-to-use prompt body for the new task is saved alongside this log at
`recurring-task-prompt.md` in this same directory.

## Carry-forward issue

Auto-posting will keep failing every Wed until Chrome MCP is reconnected. If
that's the steady state, the recurring task should be revised to either
(a) generate-only and notify, or (b) wire Telegram Bot API + Twitter API v2
credentials. Flagging as a follow-up.
