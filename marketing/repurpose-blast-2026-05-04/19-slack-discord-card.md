# Variant 19 — Slack / Discord shareable card

**Status:** DRAFT only. Per memory `feedback_discord_retired`, do not propose Discord posting tasks. Slack — share in private/internal investor groups manually.
**CTA:** anchor signal post (single)

---

## Slack-formatted card (for paste into investor group DMs / channels)

```
:chart_with_upwards_trend: *Signal of the Week — airbytehq*

> Data Infrastructure · Growth stage · US
> +866% commit velocity / 14d · 100 contributors · deploy frequency spike

In our 4200-org dataset, this pattern (deploy frequency spike on a wide contributor base) precedes announced rounds by 2 to 4 weeks.

Full breakdown + reproducible CSV:
https://signals.gitdealflow.com/blog/signal-of-the-week-2026-05-04
```

## OG card (auto-generated by `pseo-site/app/og/.../route.tsx`)

The Slack/Discord unfurl uses the existing OG image route for the blog post. Verify with:

```
curl -sI https://signals.gitdealflow.com/blog/signal-of-the-week-2026-05-04 | grep -i og:image
```

The unfurl preview must show:
- Title: "Signal of the Week: airbytehq (Data Infrastructure) – +866% commit velocity"
- Description: first 160 chars of the blog summary
- Image: 1200x630 OG card with airbytehq logo + velocity number + sector tag

If preview is broken, refresh Slack's link cache by re-pasting after a 5-minute wait, or hit Slack's `chat.unfurl` API with a force-refresh.

## Caveat — Arabic locale users

Per memory `feedback_satori_arabic_gsub_blocked`, Arabic OG images crash. Slack/Discord users will see fallback text. Don't try to ship an Arabic-locale variant of the OG card until vercel/satori upstream fix lands.

## Where to share manually

- Investor-only Slack groups where the user is already a member
- Internal company channels at investor firms (only if the user has access)
- DMs to specific Dream 100 targets (already covered by Variant 20 cold-pitch in main distribution doc)

## Don't share in

- Public Slack workspaces (counts as posting promo content; community-fit risk per memory `feedback_target_conversations_not_people`)
- Discord (channel retired)
